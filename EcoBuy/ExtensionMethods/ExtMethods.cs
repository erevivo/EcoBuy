namespace EcoBuy.ExtensionMethods
{
    static class ExtMethods
    {
        /*public static T GetCopy<T>(this T element) where T : UIElement
        {
            string saved = XamlWriter.Save(element);
            StringReader sReader = new StringReader(saved);
            XmlReader xReader = XmlReader.Create(sReader);
            UIElement newCanvas = (UIElement)XamlReader.Load(xReader);
            return (T)newCanvas;
        }*/

    }
}
