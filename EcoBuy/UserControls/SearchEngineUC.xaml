<UserControl x:Class="EcoBuy.UserControls.SearchEngineUI"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:EcoBuy.UserControls"
             mc:Ignorable="d"
             d:DesignHeight="410"
             d:DesignWidth="1220">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <Border BorderThickness="2"
                    Height="90"
                    CornerRadius="20"
                    BorderBrush="#e0e6ed"
                    Background="white"
                    Width="1100"
                    Margin="110,37,10,26">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10"
                                      ShadowDepth="6"
                                      Color="#adadad" />
                </Border.Effect>
            </Border>
            <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     VerticalAlignment="Top"
                     FlowDirection="RightToLeft"
                     FontSize="26"
                     Height="70"
                     AcceptsReturn="False"
                     TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Auto"
                     materialDesign:HintAssist.Hint="חפש מוצר"
                     IsEnabled="{Binding Path=IsChecked, ElementName=MaterialDesignOutlinedTextBoxEnabledComboBox}"
                     Margin="128,51,42,0"
                     BorderThickness="0" />
            <ListBox Grid.Row="1"
                     ItemsSource="{Binding MyFilteredItems}"></ListBox>
            <Button Style="{StaticResource MaterialDesignFloatingActionAccentButton}"
                    ToolTip="חפש"
                    Margin="10,43,1130,30"
                    Height="80"
                    Width="80">
                <materialDesign:PackIcon Kind="Magnify"
                                         Height="50"
                                         Width="50" />
            </Button>
        </Grid>
        <Grid Height="270" Width="1100">
           <local:searchResultUC Visibility="Visible" Margin="48,69,-48,-69" /> 
        </Grid>
    </Grid>
</UserControl>
